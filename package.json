{
  "name": "smis-sso",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for the SMIS SSO client library and platform gateway definitions",
  "workspaces": [
    "packages/*",
    "auth-gateway/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
